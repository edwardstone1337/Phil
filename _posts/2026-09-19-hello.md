---
layout: post
title: "Hello"
---

This is a post. To write another one, drop a new file in `_posts/` named
`YYYY-MM-DD-some-title.md`, give it the three lines of front matter at the
top, and push. That's the whole workflow.

Normal markdown works: **bold**, *italic*, [links](https://example.com),
and lists —

- one
- two
- three

> Block quotes look like this.

`Inline code` and blocks:

```
$ git push
```

That's it.
